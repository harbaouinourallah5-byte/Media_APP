require('dotenv').config({path: '.env.local'}); const mongoose = require('mongoose'); mongoose.connect(process.env.MONGODB_URI).then(async () => { const db = mongoose.connection.db; await db.collection('products').updateMany({}, { $set: { stock: 5 } }); console.log('Done!'); process.exit(0); })
